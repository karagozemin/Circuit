# Jury demo — Compose → Compile → Execute → React

Target: 3 minutes. Keep the network label visible. Hide private environment files and wallet secrets.

1. **0:00–0:20 — Position.** Landing: “Circuit gives Event Contracts a programmable strategy language.” Show the three program cards.
2. **0:20–1:00 — Compose.** Open Contrarian Roller. Select CONDITION, change its threshold. Remove and add a CONDITION node, reconnect MARKET.next and CONDITION.match. Show an incomplete connection failing compilation, then repair it. This is an editable program, not an animated preview.
3. **1:00–1:25 — Compile.** Compile the graph, inspect/download its real manifest and hash. Briefly show a partial Turkish intent: extracted rules and missing limits, with no invented budget. Save and reopen the graph.
4. **1:25–1:50 — Three programs.** Show Contrarian Roller, Conditional Ladder and Bounded Streak using the same compiler and Engine. Open the runtime proof: 3 graph-compiled manifests, one Engine address, 8 settled rounds. Explicitly label this reproducible local Anvil/mock-market experiment. Explain the distinct progression: redeemed-win rolling; 5 → 7.5 → 10 after wins; a bounded streak ending on its first loss. Use the [1m/5m/15m-capable Shannon deployment](../deployments/shannon-short-windows.json), prepare its linked account and check a live window before demonstrating activation.
5. **1:50–2:40 — Execute and React.** Activity: current state, capital cap, next budget, subscription IDs, actual fill and resolution receipts, successor MARKET transition. Explain: “DreamDEX resolution → Somnia Reactivity → Circuit state transition → next action.” A running keeper submits the bounded transactions.
6. **2:40–3:00 — Close.** Show owner control and the actual event timeline. “Same engine. Three different trading programs. Compose, compile, execute, react.”

Wallet confirmations may be sped up with an explicit label. If using the existing real lifecycle video, label it “Earlier recorded live testnet run” and identify that program; never present old proof as a new Ladder execution. Local tests and unverified stages are labelled as such. Describe the implemented bounded DSL accurately: node editing and typed connections compile to validated manifests; arbitrary graph topologies and a general-purpose VM are not implemented. Live proof for all three programs and automatic keeper hosting remain separate from the local runtime experiment.
