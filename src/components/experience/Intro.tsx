import { useState } from 'react'
import { ArrowLeft, ArrowRight, Check, GitBranch, LockKeyhole, Radio } from 'lucide-react'
import { Brand } from './shared'
const slides=[{label:'A LITTLE HELLO',title:'Meet your new',accent:'way to flow.',body:'A workspace for your ideas, your boundaries, and every market window that comes next.',Icon:GitBranch},{label:'ALWAYS YOUR CALL',title:'A clear plan.',accent:'A firm boundary.',body:'Choose your entry. Set your spending and stop limits. You review the complete strategy before your wallet authorizes anything.',Icon:LockKeyhole},{label:'FROM IDEA TO ACTION',title:'See the story.',accent:'Every step of it.',body:'Follow verified signals, orders and settlement in one place. Start with a draft — no wallet or funds needed to explore.',Icon:Radio}]
export default function Intro({onComplete,onBack}:{onComplete:()=>void;onBack:()=>void}){const [step,setStep]=useState(0);const slide=slides[step];return <div className="intro-page"><header><Brand/><button className="quiet-link" onClick={onComplete}>Skip introduction <ArrowRight size={15}/></button></header><main id="main-content" className="intro-main" key={step}><div className="intro-illustration"><div className="intro-ring ring-a"/><div className="intro-ring ring-b"/><div className="intro-center"><slide.Icon size={43} strokeWidth={1.4}/></div><span className="intro-mini mini-one"><Check size={15}/> Your rules</span><span className="intro-mini mini-two"><LockKeyhole size={14}/> Your control</span></div><span className="eyebrow">{slide.label}</span><h1>{slide.title}<br/><em>{slide.accent}</em></h1><p>{slide.body}</p><div className="intro-progress" aria-label={`Introduction step ${step+1} of 3`}>{slides.map((_,i)=><button key={i} aria-label={`Go to introduction step ${i+1}`} aria-current={i===step?'step':undefined} onClick={()=>setStep(i)} className={i===step?'current':''}/>)}</div><div className="intro-actions"><button className="icon-btn" aria-label="Previous step" onClick={()=>step?setStep(step-1):onBack()}><ArrowLeft size={20}/></button><button className="btn btn-lime btn-lg" onClick={()=>step===2?onComplete():setStep(step+1)}>{step===2?'Make yourself at home':'A little more'}<ArrowRight size={18}/></button></div></main><footer><span>01 / A workspace built around you</span><span>SOMNIA SHANNON · TESTNET</span></footer></div>}
